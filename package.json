{
  "name": "sample-nodejs-app",
  "version": "1.0.0",
  "description": "A sample Node.js application for CI/CD demonstration",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "jest",
    "integration-tests": "jest --testPathPattern=integration",
    "regression-tests": "jest --testPathPattern=regression",
    "sonar": "echo 'Running SonarQube analysis...'"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.5.0",
    "supertest": "^6.3.3"
  }
}
